/**
 * Auth-related route groups and pages.
 * Place login, signup, verify-email, forgot-password, and OAuth callbacks here.
 * Uses (auth) route group so layout can be shared without affecting URL.
 */
