/**
 * Public route group for unauthenticated pages.
 * Home, search, provider profiles, adopt landing, giving, blog, how-it-works, about.
 * Uses (public) so these share a layout without adding to the URL path.
 */
