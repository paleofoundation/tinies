/**
 * Supabase client setup and helpers.
 * - server.ts: createClient() for Server Components / Actions / Route Handlers (uses cookies).
 * - client.ts: createClient() for Client Components (browser).
 * Import from "./server" or "./client" depending on context.
 */
